{
  "name": "review-pulse",
  "version": "1.0.0",
  "description": "Web-based reputation management dashboard for small businesses",
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "NODE_ENV=production node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
