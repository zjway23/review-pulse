# Optional — without this the app uses a built-in mock AI for sentiment,
# themes, and response drafts, so everything works offline.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Server port (default 4000)
PORT=4000
